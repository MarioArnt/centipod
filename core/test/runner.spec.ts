describe('[class] Runner', () => {
  describe('[method] runCommand', () => {
    it.todo('should resolve all targets that have the command - parallel');
    it.todo('should resolve only package that have the command - parallel with argument to');
    it.todo('should resolve recursively resolve targets - topological');
  });
});
